{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "erumpay-service.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "erumpay-service.labels" . | nindent 4 }}
{{- end }}
