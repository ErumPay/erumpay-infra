apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: card-service-secret
  namespace: pay
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: erumpay-dev-secret-store
    kind: ClusterSecretStore
  target:
    name: card-service-secret
    creationPolicy: Owner
  data:
    - secretKey: DB_USERNAME
      remoteRef:
        key: erumpay/dev/all
        property: DB_USERNAME
    - secretKey: DB_PASSWORD
      remoteRef:
        key: erumpay/dev/all
        property: DB_PASSWORD
    - secretKey: CARD_BILLING_KEY_AES_KEY
      remoteRef:
        key: erumpay/dev/all
        property: CARD_BILLING_KEY_AES_KEY
