services:
  mysql:
    image: mysql:8.0
    container_name: ${MYSQL_CONTAINER_NAME}
    restart: unless-stopped

    ports:
      - '${MYSQL_PORT}:3306'

    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      TZ: ${TZ}

    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-time-zone=+09:00

    volumes:
      - mysql_data:/var/lib/mysql
      - ./mysql/init:/docker-entrypoint-initdb.d
      - ./mysql/conf.d:/etc/mysql/conf.d

    healthcheck:
      test: ['CMD-SHELL', 'mysqladmin ping -h localhost -uroot -p$${MYSQL_ROOT_PASSWORD}']
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

    networks:
      - erumpay-network

  kafka:
    image: confluentinc/cp-kafka:7.6.1
    container_name: erumpay-kafka
    restart: unless-stopped

    ports:
      - '9092:9092'

    environment:
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:29093
      KAFKA_LISTENERS: PLAINTEXT://kafka:29092,CONTROLLER://kafka:29093,PLAINTEXT_HOST://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: true

    volumes:
      - kafka_data:/var/lib/kafka/data

    healthcheck:
      test: ['CMD-SHELL', 'kafka-topics --bootstrap-server localhost:9092 --list >/dev/null 2>&1']
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

    networks:
      - erumpay-network

  redis:
    image: redis:7.2-alpine
    container_name: erumpay-redis
    restart: unless-stopped

    ports:
      - '6379:6379'

    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

    networks:
      - erumpay-network

  # billing-key-service:
  #   build:
  #     context: ../billing-key-service
  #     dockerfile: Dockerfile
  #   container_name: erumpay-billing-key-service
  #   restart: unless-stopped
  #   ports:
  #     - "8092:8092"
  #   environment:
  #     DB_URL: jdbc:mysql://mysql:3306/pg_billing_db
  #     DB_USERNAME: root
  #     DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #     CARD_SIMULATOR_BASE_URL: http://card-simulator-service:8095
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #   networks:
  #     - erumpay-network

  # card-service:
  #   build:
  #     context: ../card-service
  #     dockerfile: Dockerfile
  #   container_name: erumpay-card-service
  #   restart: unless-stopped
  #   ports:
  #     - "8082:8082"
  #   environment:
  #     DB_URL: jdbc:mysql://mysql:3306/card_db
  #     DB_USERNAME: root
  #     DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #     BILLING_KEY_SERVICE_BASE_URL: http://billing-key-service:8092
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #     billing-key-service:
  #       condition: service_started
  #   networks:
  #     - erumpay-network

  # card-simulator-service:
  #   build:
  #     context: ../card-simulator-service
  #     dockerfile: Dockerfile
  #   container_name: erumpay-card-simulator-service
  #   restart: unless-stopped
  #   ports:
  #     - "8095:8095"
  #   environment:
  #     DB_URL: jdbc:mysql://mysql:3306/simulator_db
  #     DB_USERNAME: root
  #     DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #   networks:
  #     - erumpay-network

  # payment-service:
  #   build:
  #     context: ../payment-service
  #     dockerfile: Dockerfile
  #   container_name: erumpay-payment-service
  #   restart: unless-stopped
  #   ports:
  #     - "8083:8083"
  #   environment:
  #     DB_URL: jdbc:mysql://mysql:3306/payment_db
  #     DB_USERNAME: root
  #     DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #     QR_BASEURL: http://payment-service:8083
  #     KAFKA_BOOTSTRAP_SERVERS: kafka:29092
  #     REDIS_HOST: redis
  #     REDIS_PORT: 6379
  #     AUTH_BASE_URL: http://auth-service:8081
  #     PG_BASE_URL: http://pg-payment-service:8093
  #     RECOMMEND_BASE_URL: http://recommendation-service:8084
  #     CARD_BASE_URL: http://card-service:8082
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #     kafka:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #     card-service:
  #       condition: service_started
  #     pg-payment-service:
  #       condition: service_started
  #     recommendation-service:
  #       condition: service_started
  #   networks:
  #     - erumpay-network

  # pg-payment-service:
  #   build:
  #     context: ../pg-payment-service
  #     dockerfile: Dockerfile
  #   container_name: erumpay-pg-payment-service
  #   restart: unless-stopped
  #   ports:
  #     - "8093:8093"
  #   environment:
  #     DB_URL: jdbc:mysql://mysql:3306/pg_payment_db
  #     DB_USERNAME: root
  #     DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #     billing-key-service:
  #       condition: service_started
  #     card-simulator-service:
  #       condition: service_started
  #   networks:
  #     - erumpay-network

  # recommendation-service:
  #   build:
  #     context: ../recommendation-service
  #     dockerfile: Dockerfile
  #   container_name: erumpay-recommendation-service
  #   restart: unless-stopped
  #   ports:
  #     - "8084:8084"
  #   environment:
  #     DB_URL: jdbc:mysql://mysql:3306/recommend_db
  #     DB_USERNAME: root
  #     DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #     CARD_SERVICE_BASE_URL: http://card-service:8082
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #     card-service:
  #       condition: service_started
  #   networks:
  #     - erumpay-network

volumes:
  mysql_data:
  kafka_data:

networks:
  erumpay-network:
    driver: bridge
